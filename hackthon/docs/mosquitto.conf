# Minimal Mosquitto broker config for the MQTT-based gNMI architecture.
# All clients on vpn-net connect anonymously; no TLS needed inside Docker.
allow_anonymous true
listener 1883 0.0.0.0
persistence false
log_dest stdout
log_type all
